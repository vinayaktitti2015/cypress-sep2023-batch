import * as person from "../pages/person";

// import { name, email, phone } from './person.js';

describe("test suite", () => {
  it("test case", () => {
    console.log(person.name);
    console.log(person.email);
    console.log(person.phone);
    // expect(person.name).toBe('John');
  });
});
